# 2026-07-19 프로토타입 초안

**세션:** 02:05 자율업무 · 아이디어샘물-프로토
**진행:** 마이클(개발 대리) 초안 → 클레버(개발 팀장) 반박·보완 → 합의 확정
**입력:** `2026-07-19/01-시장필터결과.md` (통과 3건)
**결과:** 3건 모두 개발 가능 판정. 탈락 0건.

---

## 합의 확정 사항

| 건 | 판정 | 클레버 조건 |
|---|---|---|
| [4] 예약관리 | ✅ 진행 | 알림 수단: 카카오톡 링크 우선, SMS 선택으로 변경 |
| [19] 회비관리 | ✅ 진행 | "미납자 알림 보내기" 메인 CTA 우선 설계 |
| [15] 세금정리 | ✅ 진행 (조건부) | **MVP 배포 전 세무사 검증 1회 필수** |

---

## [4] 소규모 비즈니스 예약 관리 + 리마인더

### 기능 명세 (MVP)

| 기능 | 설명 |
|---|---|
| 예약 등록 | 고객명·연락처·서비스·날짜시간 |
| 자동 리마인더 | 예약 1일 전 / 1시간 전 (카카오톡 링크 우선, SMS 선택) |
| 대시보드 | 오늘 예약 / 이번주 캘린더 뷰 |
| 노쇼 관리 | 노쇼 표시 + 고객별 노쇼 횟수 추적 |
| 고객 DB | 이름·연락처·방문횟수·메모 |

**"된다"의 기준:** 업주가 예약을 등록하면, 고객이 예약 전날과 1시간 전에 카카오톡 링크로 리마인더를 받는다.

### 화면 흐름

```
로그인
└─ 대시보드
   ├─ 오늘 예약 목록 (시간순, 상태 표시)
   ├─ 이번주 캘린더 (요일별 예약 수)
   └─ [+ 예약 추가]
       └─ 고객 선택 (또는 신규 등록) → 서비스 선택 → 날짜/시간 → 저장
           → 리마인더 자동 예약됨

고객 목록
└─ 이름·연락처·방문횟수·노쇼횟수
   └─ 고객 상세 → 예약 이력

설정
└─ 리마인더 발송 타이밍 (1일 전 / 1시간 전 / 둘 다)
   리마인더 메시지 템플릿
   알림 수단 (카카오톡 링크 / SMS)
```

### 데이터 구조

```typescript
type Business = {
  id: string
  name: string
  phone: string
  category: string  // 'pilates' | 'nail' | 'dental' | ...
}

type Customer = {
  id: string
  businessId: string
  name: string
  phone: string
  visitCount: number
  noShowCount: number
  note?: string
}

type Appointment = {
  id: string
  businessId: string
  customerId: string
  datetime: Date
  service: string
  status: 'booked' | 'completed' | 'noshow' | 'cancelled'
}

type Reminder = {
  id: string
  appointmentId: string
  scheduledAt: Date
  sentAt?: Date
  channel: 'kakao_link' | 'sms'
  status: 'pending' | 'sent' | 'failed'
}
```

### 코드 스켈레톤

```typescript
// app/api/appointments/route.ts
export async function POST(req: Request) {
  const { customerId, datetime, service } = await req.json()
  const appt = await db.appointment.create({
    data: { customerId, datetime, service, status: 'booked' }
  })
  await scheduleReminders(appt.id, new Date(datetime))
  return Response.json(appt)
}

async function scheduleReminders(apptId: string, dt: Date) {
  const offsets = [24 * 60 * 60 * 1000, 60 * 60 * 1000] // 1일 전, 1시간 전
  for (const offset of offsets) {
    const delay = dt.getTime() - offset - Date.now()
    if (delay > 0) {
      await reminderQueue.add('send', { apptId }, { delay })
    }
  }
}

// workers/reminder.ts
reminderQueue.process('send', async (job) => {
  const appt = await db.appointment.findUnique({
    where: { id: job.data.apptId },
    include: { customer: true, business: true }
  })
  if (!appt || appt.status !== 'booked') return

  // 카카오톡 링크: 별도 비즈니스 채널 없이 공유 가능
  const kakaoLink = buildKakaoShareLink(appt)
  await sendKakaoLink(appt.customer.phone, kakaoLink)
})

function buildKakaoShareLink(appt: Appointment & { business: Business }) {
  const msg = `[${appt.business.name}] 예약 안내\n${format(appt.datetime, 'M월 d일 HH:mm')}\n서비스: ${appt.service}`
  return `https://sharer.kakao.com/talk/friends/picker/link?text=${encodeURIComponent(msg)}`
}
```

### 기술 스택

| 레이어 | 기술 |
|---|---|
| 프론트 | Next.js 15 (App Router) + Tailwind |
| 백엔드 | Next.js API Routes |
| 저장소 | PostgreSQL (Supabase) |
| 큐 | BullMQ + Redis (리마인더 스케줄) |
| 알림 | 카카오톡 링크 공유 (1단계) → 알림톡 (2단계) |

### 예상 개발 시간 (MVP)

| 단계 | 기간 |
|---|---|
| 예약 등록·관리 UI | 2주 |
| 고객 DB | 0.5주 |
| 리마인더 큐·발송 | 1주 |
| 대시보드 | 1주 |
| 카카오톡 링크 연동 | 0.5주 |
| **합계** | **약 5주** |

---

## [19] 커뮤니티·동호회 회비 관리

### 기능 명세 (MVP)

| 기능 | 설명 |
|---|---|
| 그룹 생성 | 그룹명·회원 초대 링크 |
| 회비 항목 | 월별·분기별·특별 항목 등록 (금액·마감일) |
| 납부 현황 | 회원별 납부O/X 수동 체크 (총무만 가능) |
| 미납자 알림 | 카카오페이 송금 링크 + 메시지 카카오톡 공유 |
| 총무 대시보드 | 납부율·미납 명단·총 금액 현황 |

**"된다"의 기준:** 총무가 버튼 하나로 미납자에게 카카오페이 링크를 보낼 수 있다. 오해받을 걱정 없이 공식적으로.

**클레버 수정:** 메인 CTA는 "미납자에게 알림 보내기" — 납부율 숫자가 아니라 이 액션이 대시보드 화면 정중앙에 위치해야 한다. 총무의 심리 부담 해소가 핵심 UX.

### 화면 흐름

```
총무 로그인
└─ 대시보드 (메인)
   ├─ [미납자 X명에게 알림 보내기] ← 메인 CTA (가장 크게)
   ├─ 이번 달 납부율 72% (7/25명)
   ├─ 미납자 목록 (이름·금액)
   └─ 전체 납부 현황 보기

회비 항목 관리
└─ [+ 항목 추가] 제목·금액·기간(월별/분기/특별)·마감일

회원 관리
└─ 회원 목록 → 항목별 납부O/X 체크
   └─ 개별 수납 확인 체크

미납자 알림 보내기
└─ 미납자 목록 확인 → 메시지 편집
   → [카카오톡으로 공유] (카카오페이 송금 링크 포함)
```

### 데이터 구조

```typescript
type Group = {
  id: string
  name: string
  adminId: string
  inviteCode: string
}

type Member = {
  id: string
  groupId: string
  name: string
  phone?: string
  role: 'admin' | 'member'
}

type DueItem = {
  id: string
  groupId: string
  title: string
  amount: number
  period: 'monthly' | 'quarterly' | 'once'
  dueDate: Date
}

type Payment = {
  id: string
  memberId: string
  dueItemId: string
  paidAt: Date
  confirmedBy: string  // 총무 id
}
```

### 코드 스켈레톤

```typescript
// app/api/groups/[id]/dues-status/route.ts
export async function GET(req: Request, { params }) {
  const members = await db.member.findMany({ where: { groupId: params.id } })
  const dueItems = await db.dueItem.findMany({ where: { groupId: params.id } })
  const payments = await db.payment.findMany({
    where: { dueItem: { groupId: params.id } }
  })

  return Response.json(members.map(m => ({
    ...m,
    unpaid: dueItems.filter(d =>
      !payments.some(p => p.memberId === m.id && p.dueItemId === d.id)
    ),
  })))
}

// lib/kakao-pay.ts — 사업자 불필요, 개인도 사용 가능
function buildKakaoPayMessage(members: Member[], dueItem: DueItem) {
  const names = members.map(m => m.name).join(', ')
  const payLink = `https://qr.kakaopay.com/[개인QR]?amount=${dueItem.amount}`
  return `[${dueItem.title}] 회비 안내\n미납: ${names}\n금액: ${dueItem.amount.toLocaleString()}원\n송금: ${payLink}`
}

// 카카오톡 공유 (URL scheme)
function shareToKakao(message: string) {
  window.location.href = `kakaotalk://msg/send?text=${encodeURIComponent(message)}`
}
```

### 기술 스택

| 레이어 | 기술 |
|---|---|
| 프론트 | Next.js 15 + Tailwind |
| 백엔드 | Next.js API Routes |
| 저장소 | Supabase (PostgreSQL + Auth) |
| 결제 | 카카오페이 개인 QR 링크 (1단계) → 사업자 API (2단계) |

### 예상 개발 시간 (MVP)

| 단계 | 기간 |
|---|---|
| 그룹·회원 관리 | 1.5주 |
| 회비 항목·납부 체크 | 1주 |
| 총무 대시보드 | 1주 |
| 미납자 알림·카카오페이 링크 | 0.5주 |
| **합계** | **약 4주** |

---

## [15] 사이드 프로젝트·프리랜서 세금 정리

### 기능 명세 (MVP)

| 기능 | 설명 |
|---|---|
| 월별 수입 입력 | 수동 입력 + CSV 업로드, 원천세 여부 체크 |
| 지출 입력 | 수동 입력, 필요경비 여부 태깅 |
| 월별 대시보드 | 수입·지출·예상 세액 추이 (메인 화면) |
| 세액 자동 계산 | 기타소득(3.3%) / 사업소득 구분 계산 |
| 5월 요약 PDF | 연간 수입·필요경비·세액 요약 (부가 기능) |

**"된다"의 기준:** 매달 수입·지출을 입력하면 예상 세액이 자동 계산되고, 5월에 PDF 한 장 뽑아서 홈택스 신고 참고 자료로 쓸 수 있다.

**클레버 조건:** MVP 배포 전 세무사 검증 1회 필수 (비용 약 30~50만원). 세율 로직 자체보다 "어떤 항목이 필요경비로 인정되는가" 부분이 사용자별로 달라 오류 여지 있음. 면책 조항 문구도 세무사 확인 필요.

### 화면 흐름

```
최초 설정 (소득 유형 선택)
└─ 기타소득 (3.3% 원천징수) / 사업소득 (사업자등록 유무)

월별 대시보드 (메인 화면)
├─ 이달 수입 / 지출 / 예상 세액 카드
├─ [+ 수입 추가] 날짜·금액·출처·원천징수 여부·원천세액
├─ [+ 지출 추가] 날짜·금액·항목·필요경비 여부
└─ 연간 추이 차트 (월별 수입·누적 예상세액)

5월 신고 준비
└─ 연간 요약 (총수입·총필요경비·과세표준·예상세액·기납부세액)
   └─ [PDF 다운로드]

설정
└─ 소득 유형 변경 / 필요경비 기본 카테고리 설정
```

### 데이터 구조

```typescript
type User = {
  id: string
  incomeType: 'other_income' | 'business_income'
  hasBusinessRegistration: boolean
}

type Income = {
  id: string
  userId: string
  date: Date
  amount: number
  source: string
  withheld: boolean
  withholdingAmount?: number  // 원천징수 세액
}

type Expense = {
  id: string
  userId: string
  date: Date
  amount: number
  category: string  // '장비' | '소프트웨어' | '교통' | '통신' | ...
  isDeductible: boolean
  memo?: string
}

type TaxSummary = {
  year: number
  totalIncome: number
  totalWithheld: number
  totalDeductibleExpenses: number
  netIncome: number
  estimatedTax: number
  refundOrPayable: number  // 환급(+) 또는 추가납부(-)
}
```

### 코드 스켈레톤

```typescript
// lib/tax-calculator.ts
const TAX_BRACKETS_2024 = [
  { limit: 14_000_000,  rate: 0.06, deduction: 0 },
  { limit: 50_000_000,  rate: 0.15, deduction: 1_260_000 },
  { limit: 88_000_000,  rate: 0.24, deduction: 5_760_000 },
  { limit: 150_000_000, rate: 0.35, deduction: 15_440_000 },
  { limit: 300_000_000, rate: 0.38, deduction: 19_940_000 },
  { limit: 500_000_000, rate: 0.40, deduction: 25_940_000 },
  { limit: Infinity,    rate: 0.42, deduction: 35_940_000 },
]

export function calculateTax(totalIncome: number, deductibleExpenses: number): TaxSummary {
  const basicDeduction = 1_500_000  // 기본공제 (근로소득 외)
  const netIncome = Math.max(0, totalIncome - deductibleExpenses - basicDeduction)
  const bracket = TAX_BRACKETS_2024.find(b => netIncome <= b.limit)!
  const estimatedTax = Math.max(0, Math.floor(netIncome * bracket.rate - bracket.deduction))
  return {
    totalIncome,
    netIncome,
    estimatedTax,
    effectiveRate: totalIncome > 0 ? estimatedTax / totalIncome : 0,
  }
}

// app/api/tax/summary/route.ts
export async function GET(req: Request) {
  const { searchParams } = new URL(req.url)
  const year = parseInt(searchParams.get('year') ?? String(new Date().getFullYear()))

  const [incomes, expenses] = await Promise.all([
    db.income.findMany({ where: { userId: getUser(req), year } }),
    db.expense.findMany({ where: { userId: getUser(req), year, isDeductible: true } }),
  ])

  const totalIncome = incomes.reduce((s, i) => s + i.amount, 0)
  const totalWithheld = incomes.reduce((s, i) => s + (i.withholdingAmount ?? 0), 0)
  const totalDeductible = expenses.reduce((s, e) => s + e.amount, 0)
  const { estimatedTax } = calculateTax(totalIncome, totalDeductible)

  return Response.json({
    totalIncome, totalWithheld, totalDeductible,
    estimatedTax,
    refundOrPayable: totalWithheld - estimatedTax,  // 양수 = 환급, 음수 = 추가납부
  })
}
```

### 기술 스택

| 레이어 | 기술 |
|---|---|
| 프론트 | Next.js 15 + Tailwind + Recharts (차트) |
| 백엔드 | Next.js API Routes |
| 저장소 | PostgreSQL (Supabase) |
| PDF | @react-pdf/renderer |
| 홈택스 연동 | **MVP 배제** (수동 입력으로 대체) |

### 예상 개발 시간 (MVP)

| 단계 | 기간 |
|---|---|
| 수입·지출 입력 + CSV | 2주 |
| 세액 계산 로직 | 1.5주 |
| 월별 대시보드 | 1.5주 |
| 5월 요약 PDF | 1주 |
| 세무사 검증 대응 수정 | 1주 |
| **합계** | **약 7주** |

---

## 개발팀 종합 판단

| 건 | 난이도 | 우선순위 추천 | 비고 |
|---|---|---|---|
| [19] 회비관리 | 낮음 | 1순위 | 가장 빠른 MVP 가능 (4주). 기술 리스크 최소 |
| [4] 예약관리 | 중간 | 2순위 | 5주. 카카오 알림톡 심사 지연만 주의 |
| [15] 세금정리 | 높음 | 3순위 | 7주 + 세무사 검증. 시장은 검증됨, 정확도 관리가 관건 |

**다음 단계:** 03:05 디자인팀 — 이 초안 참조.

---

**작성:** 마이클(개발 대리) 초안 + 클레버(개발 팀장) 반박·보완 합의
**확정:** 2026-07-19 02:05 KST
